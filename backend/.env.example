# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# Para desenvolvimento local (sem Docker): use sqlite3
# Para Docker: use postgresql
DB_ENGINE=django.db.backends.postgresql
DB_NAME=petkeep_db
DB_USER=petkeep_user
DB_PASSWORD=petkeep_password
DB_HOST=db  # Use 'db' para Docker ou 'localhost' para desenvolvimento local
DB_PORT=5432

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:19006
